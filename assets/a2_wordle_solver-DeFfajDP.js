import{n as e,o as t,r as n}from"./index-BpPtaAoR.js";import{n as r,t as i}from"./text-x5hBlD8M.js";var a=`/assets/model_score_distribution-Bzwof_t3.png`,o=t(n(),1);const s={title:`Solving Wordle: A Novice Approach`,description:`An exploratory investigation into solving Wordle using greedy heuristics and dynamic programming.`,date:`2025-12-28`,tags:[`algorithms`,`python`,`dynamic programming`,`information theory`,`fun`,`off topic`],type:`article`,slug:`a2_wordle_solver`};function c(){return(0,o.jsxs)(e,{children:[(0,o.jsx)(i,{lead:`Wordle is a viral daily word puzzle where the player has six attempts to find a hidden five letter word. After each guess, the game tells the user some information about their guess: If each letter is either correct, in the word but not in the right place, or not in the word at all.`,content:`Despite being a simple game that almost anyone can play, Wordle is an interesting problem to investigate from a data and algorithmic perspective. Each guess provides information that constrains the remaining solution space, making the game a small but concrete example of inference under uncertainty.`}),(0,o.jsx)(i,{heading:`Methodology`,lead:`To be able to test a solving algorithm, I first had to reproduce a copy of the game.`,content:`After this, I could plug an algorithm into the game's inputs and outputs to make guesses and observe feedback.
                From there, the only remaining task was to design the algorithm itself, starting with an obvious first approach: a letter frequency based strategy.`}),(0,o.jsx)(i,{content:`Wordle draws its solutions from a fixed list of valid words, which was originally accessible in the JavaScript source of the game. For this investigation, I restrict candidate solutions to that same list. While this simplifies the solver, it also allows performance to be measured precisely over the same solution space used by the game itself.`}),(0,o.jsx)(i,{heading:`A Greedy Letter Frequency Approach`,lead:`A natural first strategy is to favour guesses that test the most common letters across the remaining candidate words.`,content:`At any point in the game, the feedback received so far determines a set of remaining candidate words.
The goal of the next guess is therefore not necessarily to be correct, but to extract as much information as possible.`,bullets:[`Step 1: Maintain a set of candidate words that are still consistent with all feedback seen so far.`,`Step 2: Count how often each letter appears across those candidates.`,`Step 3: Score each possible guess based on those letter frequencies.`,`Step 4: Choose the highest scoring word as the next guess.`]}),(0,o.jsx)(i,{content:`Step 1: After each guess, reduce the candidate set by removing any words that are inconsistent with the observed feedback.`,code:`candidates ← all valid words

for each guess:
    feedback ← compare(guess, answer)
    candidates ← words consistent with feedback`}),(0,o.jsx)(i,{content:`Step 2: Build a frequency table by scanning the remaining candidates.
Each letter is only counted once per word, so repeated letters do not artificially inflate a word’s score.`,code:`frequency_table ← empty map

for each word in candidates:
    for each unique letter in word:
        frequency_table[letter] += 1`}),(0,o.jsx)(i,{content:`Step 3: Assign a score to each possible guess by summing the frequencies of its unique letters.
Words that cover common letters across the candidate set receive higher scores.`,code:`score(word) =
    sum of frequency_table[letter]
    for each unique letter in word`}),(0,o.jsx)(i,{content:`Step 4: Select the word with the highest score.
If multiple words tie, choose one deterministically to keep results reproducible.`,code:`best_guess ← word with maximum score
play best_guess`}),(0,o.jsx)(i,{content:`Step 3: Score a word by adding up the frequencies of its unique letters. Words that contain common letters will score higher.`,code:`score = sum(freq[ch] for ch in set(word))`}),(0,o.jsx)(i,{content:`Step 4: Pick the highest scoring candidate word. Using sorted(...) makes the selection deterministic when multiple words tie for the same score.`,code:`best_guess = max(sorted(candidates), key=score)`}),(0,o.jsx)(i,{content:`This approach performs surprisingly well. Across the full set of valid Wordle answers,
                 the frequency based solver achieves a mean score of 3.482 guesses, which is quite strong performance considering it's ease of implementation. But there's a catch!`}),(0,o.jsx)(i,{heading:`Where the Letter Frequency Approach Falls Flat`,lead:`Despite its effectiveness, the letter frequency strategy only considers immediate information.`,content:`This can lead to locally optimal decisions that produce longer solution paths later in the game. 
A greedy guess can look informative (lots of greens/yellows), while still failing to reduce the candidate space meaningfully.`}),(0,o.jsx)(i,{content:`To see why, consider an endgame situation where earlier guesses have already narrowed the solution down.
Suppose the remaining candidates mostly share the same suffix:`,code:`Remaining candidates (example):
WATCH
CATCH
HATCH
MATCH
...`}),(0,o.jsx)(i,{content:`Now assume the true answer is WATCH. A frequency-based heuristic may be attracted to guesses that overlap heavily with the candidates, because they tend to score highly.`}),(0,o.jsx)(i,{content:`If you trust the frequency heuristic, and you guess CATCH, you get a pattern that looks very informative: four green letters.`,code:`Guess:
CATCH

Feedback:
BGGGG`}),(0,o.jsx)(i,{content:`The problem is that BGGGG doesn't actually isolate the answer. It confirms the “ATCH” structure, but many candidates still remain valid (HATCH, MATCH etc.). 
You’ve learned a lot about the form of the word, but you haven’t reduced the remaining options as much as you might expect.`}),(0,o.jsx)(i,{content:`Now consider a different guess. Even though WALTZ contains less “popular” letters (including Z), it tests a more discriminating idea: whether the word starts with W.`,code:`Alternative guess:
WALTZ`}),(0,o.jsx)(i,{content:`If the W is confirmed, WATCH becomes much more likely. If the W is rejected, then a large chunk of candidates are eliminated immediately.
The key point is that this guess can reduce the candidate set more aggressively, even if the feedback pattern looks less “close” to the final answer.`,code:`Interpretation (informal):
If W is confirmed  → WATCH-type words survive
If W is rejected   → WATCH is eliminated, and the set collapses`}),(0,o.jsx)(i,{content:`This is the weakness of greedy scoring: it optimises for immediate overlap and common letters, not for how well a guess splits the remaining candidate space.
So how can we better select guesses based on future game states?`}),(0,o.jsx)(i,{heading:`Introducing Dynamic Programming`,lead:`Dr Tony Smith's words still echo in my mind: Dynamic programming is useful when a problem can be broken down into smaller subproblems whose solutions can be reused.`,content:`In the context of Wordle, each game state can be defined by the set of remaining candidate words. Choosing guesses partitions this set into smaller subsets based on the possible feedback patterns. By estimating the expected number of future guesses required for each subset, it becomes possible to select guesses that optimise overall performance, rather than immediate gain.`}),(0,o.jsx)(i,{content:`At the core of the dynamic programming approach is a recursive evaluation of future game states.
                Each state is defined by the remaining candidate words, and the cost of a guess is the expected number of guesses required to finish the puzzle from that state.`,code:`expected = 1.0
    for subset in partitions:
    probability = len(subset) / len(candidates)
    expected += probability * dp_value(subset)`}),(0,o.jsx)(i,{heading:`How far should we look ahead? Introducing the DP Limit`,content:`Fully evaluating all future guess sequences is computationally expensive. In fact, you can see in the code where I added testing features to limit problem size simply because it was taking too long to compute anwsers!
                To keep things workable, I created a dynamic programming limit that activates the DP strategy only once the number of remaining candidates falls below a chosen threshold. Early in the game, when the solution space is large, the solver behaves greedily. Later, when decisions become more sensitive, it switches to a more deliberate planning approach.`}),(0,o.jsx)(i,{content:`To keep the problem computationally manageable, the solver only applies dynamic programming once the number of remaining candidates falls below a fixed threshold.`,code:`if len(candidates) <= DP_LIMIT:
    _, guess = dp_value(candidates)
        else:
    guess = select_guess_frequency(candidates)`}),(0,o.jsx)(i,{lead:`But what should that threshold be?`,content:`I made a separate script to iterate through a range of limit values , and with this I was able to determine that performance doesn't increase beyond a DP limit of 121, for my data set.`}),(0,o.jsx)(i,{heading:`Results`,lead:`With the optimised DP limit in place, the dynamic programming approach does outperformed the greedy frequency model: It completes all the puzzles with a mean score of 3.395 guesses. That might not seems like much, but `,content:`While the absolute improvement in average guesses is modest, it is consistent across the full answer set. This suggests that limited lookahead can meaningfully improve decision quality, even in a relatively small problem like Wordle.`}),(0,o.jsx)(r,{src:a,alt:`Distribution of Wordle solve attempts for frequency and DP models`,className:`my-8 mx-auto`}),(0,o.jsx)(i,{content:`This chart shows how often each model solved the puzzle in 1 to 6 guesses across the full valid word set.
                The dynamic programming model shifts probability mass toward earlier solves, scoring more 3s, and less 4s and 5s.`}),(0,o.jsx)(i,{heading:`Limitations and Further Improvements`,lead:`This investigation makes several simplifying assumptions.`,content:`In particular, the solver is restricted to guessing only valid solution words. Other approaches allow guesses from a much larger dictionary, using so called probe words to extract information more efficiently. Additionally, the model assumes no knowledge of previously used Wordle solutions, although filtering recent answers could plausibly improve real-world performance, by betting that a recently used answer won't reappear in the puzzle.`}),(0,o.jsx)(i,{heading:`Conclusion`,lead:`Although this approach does not produce the absolute highest performing Wordle solver, it demonstrates how even a simple game can be used to explore ideas from greedy algorithms, information reduction, and dynamic programming.`,content:`More importantly, it highlights how modest increases in algorithmic sophistication can produce measurable improvements, and how empirical testing is essential for understanding where additional complexity actually provides meaningful benefits.`})]})}export{c as default,s as meta};